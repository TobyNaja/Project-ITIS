# config/allowlist.yaml — Protected source IPs (Blueprint §3.3)
#
# source IP ที่อยู่ในรายการนี้:
#   - Rule Engine: RULE-003 -> NO_AUTO_BLOCK + ALERT + AUDIT (ไม่ auto-block)
#   - Risk Model:  factor C = 0 (แต่ risk score ยังคำนวณตามปกติ ไม่เป็น 0)
#
# strict: IP ที่สะกดผิด -> AllowlistError ทันที (ไม่ข้ามเงียบ)
# default policy = ว่าง: ไม่มี source IP ใดถูกยกเว้นจาก auto-block

allowlist: []
